name: ULTIMATE FOOLPROOF IPA BUILDER + SIGNER

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    
    steps:
      # ============================================================
      # STEP 1: CHECKOUT WITH FULL HISTORY
      # ============================================================
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: false
          fetch-depth: 0  # Get all history to ensure all files

      # ============================================================
      # STEP 2: LIST ALL FILES (DEBUG)
      # ============================================================
      - name: List all files
        run: |
          echo "=========================================="
          echo "FILES IN REPOSITORY:"
          echo "=========================================="
          ls -la
          echo "=========================================="
          echo "FILE COUNT:"
          find . -type f | wc -l
          echo "=========================================="
          echo "DIRECTORY STRUCTURE:"
          find . -type d | head -30
          echo "=========================================="
          echo "CERTIFICATE FILES:"
          ls -la *.p12 2>/dev/null || echo "No .p12 files found"
          echo "=========================================="
          echo "PROFILE FILES:"
          ls -la *.mobileprovision 2>/dev/null || echo "No .mobileprovision files found"
          echo "=========================================="
          echo "IOS FOLDER:"
          ls -la ios/ 2>/dev/null || echo "No ios folder"
          echo "=========================================="
          echo "NODE_MODULES:"
          ls -la node_modules/ 2>/dev/null | head -10 || echo "No node_modules folder"

      # ============================================================
      # STEP 3: SETUP NODE 20
      # ============================================================
      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      # ============================================================
      # STEP 4: INSTALL DEPENDENCIES (TRY 4 METHODS)
      # ============================================================
      - name: Install dependencies (MULTIPLE ATTEMPTS)
        run: |
          echo "=== CHECKING PACKAGE.JSON ==="
          cat package.json | head -20
          
          echo "=== ATTEMPT 1: npm install --force ==="
          npm install --force && echo "✅ INSTALL SUCCESS" || echo "❌ INSTALL FAILED"
          
          if [ $? -ne 0 ]; then
            echo "=== ATTEMPT 2: npm install --legacy-peer-deps ==="
            npm install --legacy-peer-deps && echo "✅ INSTALL SUCCESS" || echo "❌ INSTALL FAILED"
          fi
          
          if [ $? -ne 0 ]; then
            echo "=== ATTEMPT 3: npm cache clean && npm install ==="
            npm cache clean --force
            npm install && echo "✅ INSTALL SUCCESS" || echo "❌ INSTALL FAILED"
          fi
          
          if [ $? -ne 0 ]; then
            echo "=== ATTEMPT 4: npm ci --force ==="
            npm ci --force && echo "✅ INSTALL SUCCESS" || echo "❌ INSTALL FAILED"
          fi
          
          echo "=== FINAL CHECK ==="
          npm list --depth=0 || echo "⚠️ Some dependencies missing but continuing"
          
          echo "=== NODE_MODULES SIZE ==="
          du -sh node_modules/ 2>/dev/null || echo "No node_modules"

      # ============================================================
      # STEP 5: CREATE/FIX APP.JSON
      # ============================================================
      - name: Create/fix app.json
        run: |
          echo "=== CHECKING app.json ==="
          if [ ! -f "app.json" ]; then
            echo "Creating app.json..."
            cat > app.json << 'EOF'
          {
            "expo": {
              "name": "gymapp2",
              "slug": "gymapp2",
              "version": "1.0.0",
              "platforms": ["ios", "web"],
              "ios": {
                "bundleIdentifier": "com.yourcompany.gymapp2",
                "supportsTablet": true
              }
            }
          }
          EOF
          else
            echo "app.json exists, fixing..."
            cat > app.json << 'EOF'
          {
            "expo": {
              "name": "gymapp2",
              "slug": "gymapp2",
              "version": "1.0.0",
              "platforms": ["ios", "web"],
              "ios": {
                "bundleIdentifier": "com.yourcompany.gymapp2",
                "supportsTablet": true
              }
            }
          }
          EOF
          fi
          
          echo "✅ app.json ready:"
          cat app.json

      # ============================================================
      # STEP 6: CHECK IOS FOLDER - USE EXISTING OR GENERATE
      # ============================================================
      - name: Check iOS folder
        run: |
          echo "=== CHECKING FOR EXISTING IOS FOLDER ==="
          
          # Check if iOS folder exists in repo
          if [ -d "ios" ] && [ -f "ios/Podfile" ]; then
            echo "✅ iOS folder already exists in repo!"
            echo "Using existing iOS folder..."
            ls -la ios/
            echo "IOS_EXISTS=true" >> $GITHUB_ENV
          else
            echo "❌ iOS folder not found or incomplete!"
            echo "Will generate iOS folder..."
            echo "IOS_EXISTS=false" >> $GITHUB_ENV
          fi

      # ============================================================
      # STEP 7: GENERATE IOS FOLDER (IF NEEDED)
      # ============================================================
      - name: Generate iOS folder (if needed)
        if: env.IOS_EXISTS == 'false'
        run: |
          export CI=1
          export EXPO_NO_INTERACTIVE=1
          
          rm -rf ios
          rm -rf node_modules/.cache
          
          echo "=== METHOD 1: expo prebuild with YES ==="
          echo "y" | npx expo prebuild --platform ios --clean && echo "✅ METHOD 1 SUCCESS" || echo "❌ METHOD 1 FAILED"
          
          if [ ! -d "ios" ]; then
            echo "=== METHOD 2: expo prebuild with CI=1 ==="
            CI=1 npx expo prebuild --platform ios --clean && echo "✅ METHOD 2 SUCCESS" || echo "❌ METHOD 2 FAILED"
          fi
          
          if [ ! -d "ios" ]; then
            echo "=== METHOD 3: react-native init ==="
            npx react-native init temp_ios --version 0.74.0 --skip-install
            cp -R temp_ios/ios .
            cp -R temp_ios/ios/* ios/ 2>/dev/null || true
            rm -rf temp_ios
            echo "✅ METHOD 3 ATTEMPTED"
          fi
          
          if [ ! -d "ios" ]; then
            echo "=== METHOD 4: Manual iOS folder creation ==="
            mkdir -p ios
            echo "platform :ios, '13.0'" > ios/Podfile
            echo "require_relative '../node_modules/@react-native-community/cli-platform-ios/native_modules'" >> ios/Podfile
            echo "target 'gymapp2' do" >> ios/Podfile
            echo "  config = use_native_modules!" >> ios/Podfile
            echo "  use_react_native!(:path => config['reactNativePath'])" >> ios/Podfile
            echo "end" >> ios/Podfile
            echo "✅ METHOD 4 ATTEMPTED"
          fi
          
          echo "=== IOS FOLDER STATUS ==="
          if [ -d "ios" ]; then
            echo "✅ iOS folder exists!"
            ls -la ios/
          else
            echo "❌ iOS folder missing!"
            exit 1
          fi

      # ============================================================
      # STEP 8: FIX PODFILE IF NEEDED
      # ============================================================
      - name: Fix Podfile if needed
        run: |
          cd ios || exit 1
          
          if [ ! -f "Podfile" ]; then
            echo "Creating Podfile..."
            echo "platform :ios, '13.0'" > Podfile
            echo "require_relative '../node_modules/@react-native-community/cli-platform-ios/native_modules'" >> Podfile
            echo "target 'gymapp2' do" >> Podfile
            echo "  config = use_native_modules!" >> Podfile
            echo "  use_react_native!(:path => config['reactNativePath'])" >> Podfile
            echo "end" >> Podfile
          else
            echo "Podfile exists, checking contents..."
            cat Podfile | head -20
          fi
          
          cd ..

      # ============================================================
      # STEP 9: INSTALL PODS (TRY 4 METHODS)
      # ============================================================
      - name: Install Pods (MULTIPLE ATTEMPTS)
        run: |
          sudo gem install cocoapods
          cd ios || exit 1
          
          echo "=== POD INSTALL ATTEMPT 1: pod install ==="
          pod install && echo "✅ PODS SUCCESS" || echo "❌ PODS FAILED"
          
          if [ $? -ne 0 ]; then
            echo "=== POD INSTALL ATTEMPT 2: pod install --repo-update ==="
            pod install --repo-update && echo "✅ PODS SUCCESS" || echo "❌ PODS FAILED"
          fi
          
          if [ $? -ne 0 ]; then
            echo "=== POD INSTALL ATTEMPT 3: pod install --verbose ==="
            pod install --verbose && echo "✅ PODS SUCCESS" || echo "❌ PODS FAILED"
          fi
          
          if [ $? -ne 0 ]; then
            echo "=== POD INSTALL ATTEMPT 4: pod cache clean --all && pod install ==="
            pod cache clean --all
            pod install && echo "✅ PODS SUCCESS" || echo "❌ PODS FAILED"
          fi
          
          echo "=== PODS STATUS ==="
          if [ -f "Pods/Pods.xcodeproj/project.pbxproj" ]; then
            echo "✅ Pods installed successfully!"
            echo "Pods size: $(du -sh Pods | cut -f1)"
          else
            echo "❌ Pods may not be installed correctly"
          fi
          
          cd ..

      # ============================================================
      # STEP 10: FIND WORKSPACE OR PROJECT
      # ============================================================
      - name: Find workspace/project
        id: find_workspace
        run: |
          cd ios || exit 1
          
          WORKSPACE=$(find . -maxdepth 1 -name "*.xcworkspace" -type d | head -1)
          PROJECT=$(find . -maxdepth 1 -name "*.xcodeproj" -type d | head -1)
          
          if [ -n "$WORKSPACE" ]; then
            SCHEME=$(basename "$WORKSPACE" .xcworkspace)
            echo "FOUND_WORKSPACE=$WORKSPACE" >> $GITHUB_ENV
            echo "FOUND_SCHEME=$SCHEME" >> $GITHUB_ENV
            echo "BUILD_TYPE=workspace" >> $GITHUB_ENV
            echo "✅ Found workspace: $WORKSPACE with scheme: $SCHEME"
          elif [ -n "$PROJECT" ]; then
            SCHEME=$(basename "$PROJECT" .xcodeproj)
            echo "FOUND_PROJECT=$PROJECT" >> $GITHUB_ENV
            echo "FOUND_SCHEME=$SCHEME" >> $GITHUB_ENV
            echo "BUILD_TYPE=project" >> $GITHUB_ENV
            echo "✅ Found project: $PROJECT with scheme: $SCHEME"
          else
            echo "❌ No iOS project found!"
            ls -la
            exit 1
          fi
          
          cd ..

      # ============================================================
      # STEP 11: BUILD APP WITH LOGGING (TRY 3 METHODS)
      # ============================================================
      - name: Build iOS app (MULTIPLE ATTEMPTS)
        run: |
          cd ios || exit 1
          
          rm -rf build
          
          if [ "$BUILD_TYPE" = "workspace" ]; then
            echo "=== BUILD ATTEMPT 1: xcodebuild with workspace ==="
            xcodebuild -workspace "$FOUND_WORKSPACE" -scheme "$FOUND_SCHEME" -configuration Release -derivedDataPath build -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -verbose 2>&1 | tee build_attempt1.log
            BUILD_RESULT=${PIPESTATUS[0]}
            echo "Build result: $BUILD_RESULT"
            
            if [ $BUILD_RESULT -eq 0 ]; then
              echo "✅ BUILD SUCCESS"
              echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "❌ BUILD FAILED"
              echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
            fi
          else
            echo "=== BUILD ATTEMPT 1: xcodebuild with project ==="
            xcodebuild -project "$FOUND_PROJECT" -scheme "$FOUND_SCHEME" -configuration Release -derivedDataPath build -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -verbose 2>&1 | tee build_attempt1.log
            BUILD_RESULT=${PIPESTATUS[0]}
            echo "Build result: $BUILD_RESULT"
            
            if [ $BUILD_RESULT -eq 0 ]; then
              echo "✅ BUILD SUCCESS"
              echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "❌ BUILD FAILED"
              echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
            fi
          fi
          
          # If first attempt failed, try second
          if [ "$BUILD_SUCCESS" != "true" ]; then
            echo "=== BUILD ATTEMPT 2: xcodebuild with clean ==="
            xcodebuild clean -workspace "$FOUND_WORKSPACE" -scheme "$FOUND_SCHEME" || xcodebuild clean -project "$FOUND_PROJECT" -scheme "$FOUND_SCHEME"
            
            if [ "$BUILD_TYPE" = "workspace" ]; then
              xcodebuild -workspace "$FOUND_WORKSPACE" -scheme "$FOUND_SCHEME" -configuration Release -derivedDataPath build -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -verbose 2>&1 | tee build_attempt2.log
              BUILD_RESULT=${PIPESTATUS[0]}
            else
              xcodebuild -project "$FOUND_PROJECT" -scheme "$FOUND_SCHEME" -configuration Release -derivedDataPath build -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO -verbose 2>&1 | tee build_attempt2.log
              BUILD_RESULT=${PIPESTATUS[0]}
            fi
            
            if [ $BUILD_RESULT -eq 0 ]; then
              echo "✅ BUILD SUCCESS"
              echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "❌ BUILD FAILED"
              echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
            fi
          fi
          
          # If second attempt failed, try third
          if [ "$BUILD_SUCCESS" != "true" ]; then
            echo "=== BUILD ATTEMPT 3: xcodebuild with additional flags ==="
            if [ "$BUILD_TYPE" = "workspace" ]; then
              xcodebuild -workspace "$FOUND_WORKSPACE" -scheme "$FOUND_SCHEME" -configuration Release -derivedDataPath build -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO OTHER_SWIFT_FLAGS="-DDEBUG" ONLY_ACTIVE_ARCH=NO -verbose 2>&1 | tee build_attempt3.log
              BUILD_RESULT=${PIPESTATUS[0]}
            else
              xcodebuild -project "$FOUND_PROJECT" -scheme "$FOUND_SCHEME" -configuration Release -derivedDataPath build -sdk iphoneos CODE_SIGNING_ALLOWED=NO CODE_SIGNING_REQUIRED=NO OTHER_SWIFT_FLAGS="-DDEBUG" ONLY_ACTIVE_ARCH=NO -verbose 2>&1 | tee build_attempt3.log
              BUILD_RESULT=${PIPESTATUS[0]}
            fi
            
            if [ $BUILD_RESULT -eq 0 ]; then
              echo "✅ BUILD SUCCESS"
              echo "BUILD_SUCCESS=true" >> $GITHUB_ENV
            else
              echo "❌ BUILD FAILED"
              echo "BUILD_SUCCESS=false" >> $GITHUB_ENV
            fi
          fi
          
          cd ..

      # ============================================================
      # STEP 12: FIND .APP (SEARCH EVERYWHERE)
      # ============================================================
      - name: Find .app file
        id: find_app
        run: |
          echo "=== SEARCHING FOR .APP FILE ==="
          
          APP_PATH=$(find ios/build -name "*.app" -type d 2>/dev/null | head -1)
          
          if [ -z "$APP_PATH" ]; then
            APP_PATH=$(find . -maxdepth 3 -name "*.app" -type d | grep -v "Pods" | head -1)
          fi
          
          if [ -z "$APP_PATH" ]; then
            APP_PATH=$(find ~/Library/Developer/Xcode/DerivedData -name "*.app" -type d 2>/dev/null | head -1)
          fi
          
          if [ -z "$APP_PATH" ]; then
            APP_PATH=$(find . -name "*.app" -type d | head -1)
          fi
          
          if [ -z "$APP_PATH" ]; then
            echo "❌ No .app found anywhere!"
            echo "=== SEARCH RESULTS ==="
            find . -name "*.app" -type d
            echo "=== BUILD FOLDER CONTENTS ==="
            find ios/build -type d 2>/dev/null || echo "No build folder"
            exit 1
          fi
          
          echo "✅ Found app at: $APP_PATH"
          echo "APP_PATH=$APP_PATH" >> $GITHUB_ENV
          
          # Get app size
          APP_SIZE=$(du -sh "$APP_PATH" | cut -f1)
          echo "App size: $APP_SIZE"

      # ============================================================
      # STEP 13: VERIFY APP IS VALID
      # ============================================================
      - name: Verify app
        run: |
          echo "=== APP CONTENTS ==="
          ls -la "${{ env.APP_PATH }}"
          echo "=== APP SIZE ==="
          du -sh "${{ env.APP_PATH }}"
          
          if [ -f "${{ env.APP_PATH }}/Info.plist" ]; then
            echo "✅ Info.plist found!"
          else
            echo "❌ Info.plist missing!"
          fi

      # ============================================================
      # STEP 14: CREATE UNSIGNED IPA
      # ============================================================
      - name: Create unsigned IPA
        run: |
          echo "=== PACKAGING IPA ==="
          rm -rf Payload
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          
          echo "=== PAYLOAD CONTENTS ==="
          ls -la Payload/
          du -sh Payload/
          
          zip -r gymapp2-unsigned.ipa Payload/
          
          # Check IPA size
          IPA_SIZE=$(stat -f%z "gymapp2-unsigned.ipa" 2>/dev/null || stat -c%s "gymapp2-unsigned.ipa" 2>/dev/null)
          echo "Unsigned IPA size: $IPA_SIZE bytes"
          
          if [ "$IPA_SIZE" -lt 1000000 ]; then
            echo "❌ IPA is too small ($IPA_SIZE bytes)! Build failed."
            echo "=== PAYLOAD CONTENTS ==="
            ls -la Payload/
            echo "=== APP CONTENTS ==="
            ls -la Payload/*.app/ 2>/dev/null || echo "No app contents"
            exit 1
          fi
          
          echo "✅ Unsigned IPA created successfully!"

      # ============================================================
      # STEP 15: DOWNLOAD ZSIGN
      # ============================================================
      - name: Download zsign
        run: |
          echo "=== DOWNLOADING ZSIGN ==="
          curl -L -o zsign https://github.com/zhlynn/zsign/releases/latest/download/zsign-macos
          chmod +x zsign
          
          if [ -f "zsign" ]; then
            echo "✅ zsign downloaded successfully!"
            ls -la zsign
          else
            echo "❌ zsign download failed!"
            exit 1
          fi

      # ============================================================
      # STEP 16: FIND CERTIFICATE AND PROFILE
      # ============================================================
      - name: Find certificate and profile
        id: find_cert
        run: |
          echo "=== FINDING CERTIFICATE ==="
          
          CERT_FILE=$(ls *.p12 2>/dev/null | head -1)
          if [ -z "$CERT_FILE" ]; then
            echo "⚠️ No .p12 certificate found - skipping signing"
            echo "SKIP_SIGN=true" >> $GITHUB_ENV
          else
            echo "✅ Found certificate: $CERT_FILE"
            echo "CERT_FILE=$CERT_FILE" >> $GITHUB_ENV
          fi
          
          echo "=== FINDING PROVISIONING PROFILE ==="
          PROFILE_FILE=$(ls *.mobileprovision 2>/dev/null | head -1)
          if [ -z "$PROFILE_FILE" ]; then
            echo "⚠️ No .mobileprovision file found - skipping signing"
            echo "SKIP_SIGN=true" >> $GITHUB_ENV
          else
            echo "✅ Found profile: $PROFILE_FILE"
            echo "PROFILE_FILE=$PROFILE_FILE" >> $GITHUB_ENV
          fi

      # ============================================================
      # STEP 17: SIGN THE IPA (TRY 3 METHODS)
      # ============================================================
      - name: Sign IPA (MULTIPLE ATTEMPTS)
        if: env.SKIP_SIGN != 'true'
        run: |
          echo "=========================================="
          echo "SIGNING IPA"
          echo "=========================================="
          echo "Certificate: ${{ env.CERT_FILE }}"
          echo "Profile: ${{ env.PROFILE_FILE }}"
          echo "=========================================="
          
          # Method 1: Standard signing
          echo "=== METHOD 1: Standard signing ==="
          ./zsign -k "${{ env.CERT_FILE }}" -p "Uhjiply-7643" -m "${{ env.PROFILE_FILE }}" -o "gymapp2-signed.ipa" "gymapp2-unsigned.ipa" && echo "✅ SIGNING SUCCESS" || echo "❌ SIGNING FAILED"
          
          # Method 2: With compression
          if [ ! -f "gymapp2-signed.ipa" ] || [ ! -s "gymapp2-signed.ipa" ]; then
            echo "=== METHOD 2: Signing with compression ==="
            ./zsign -k "${{ env.CERT_FILE }}" -p "Uhjiply-7643" -m "${{ env.PROFILE_FILE }}" -z 9 -o "gymapp2-signed.ipa" "gymapp2-unsigned.ipa" && echo "✅ SIGNING SUCCESS" || echo "❌ SIGNING FAILED"
          fi
          
          # Method 3: With verbose output
          if [ ! -f "gymapp2-signed.ipa" ] || [ ! -s "gymapp2-signed.ipa" ]; then
            echo "=== METHOD 3: Signing with verbose ==="
            ./zsign -v -k "${{ env.CERT_FILE }}" -p "Uhjiply-7643" -m "${{ env.PROFILE_FILE }}" -o "gymapp2-signed.ipa" "gymapp2-unsigned.ipa" && echo "✅ SIGNING SUCCESS" || echo "❌ SIGNING FAILED"
          fi
          
          # Check if signed IPA was created
          if [ -f "gymapp2-signed.ipa" ] && [ -s "gymapp2-signed.ipa" ]; then
            SIZE=$(stat -f%z "gymapp2-signed.ipa" 2>/dev/null || stat -c%s "gymapp2-signed.ipa" 2>/dev/null)
            echo "✅ Signed IPA created! Size: $SIZE bytes"
          else
            echo "⚠️ Signing failed, unsigned IPA will be uploaded"
          fi

      # ============================================================
      # STEP 18: UPLOAD UNSIGNED IPA
      # ============================================================
      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: gymapp2-unsigned-ipa
          path: gymapp2-unsigned.ipa
          compression-level: 9
          retention-days: 30

      # ============================================================
      # STEP 19: UPLOAD SIGNED IPA
      # ============================================================
      - name: Upload signed IPA
        if: env.SKIP_SIGN != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: gymapp2-signed-ipa
          path: gymapp2-signed.ipa
          compression-level: 9
          retention-days: 30

      # ============================================================
      # STEP 20: UPLOAD BUILD LOGS (ALWAYS)
      # ============================================================
      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            ios/build_attempt1.log
            ios/build_attempt2.log
            ios/build_attempt3.log
            ios/build/**/*.log
            ios/Podfile.lock
            ios/Pods/
            package.json
            app.json
          retention-days: 7

      # ============================================================
      # STEP 21: UPLOAD BUILD FOLDER (FOR DEBUGGING)
      # ============================================================
      - name: Upload build folder
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-folder
          path: ios/build/
          retention-days: 7

      # ============================================================
      # STEP 22: SUCCESS MESSAGE
      # ============================================================
      - name: Build and sign complete!
        if: success()
        run: |
          echo "============================================================"
          echo "🎉🎉🎉 BUILD AND SIGN COMPLETE! 🎉🎉🎉"
          echo "============================================================"
          echo "📦 UNSIGNED IPA: gymapp2-unsigned-ipa (Backup)"
          if [ -f "gymapp2-signed.ipa" ]; then
            echo "📦 SIGNED IPA: gymapp2-signed-ipa (READY TO INSTALL)"
          fi
          echo "============================================================"
          echo "NEXT STEPS:"
          if [ -f "gymapp2-signed.ipa" ]; then
            echo "1. Download gymapp2-signed-ipa from Artifacts"
            echo "2. AirDrop to your iPhone"
            echo "3. Open with AltStore"
            echo "4. Tap Install"
          else
            echo "1. Download gymapp2-unsigned-ipa from Artifacts"
            echo "2. Sign manually with zsign on Windows:"
            echo "   zsign -k certificate.p12 -p password -m profile.mobileprovision -o gymapp2-signed.ipa gymapp2-unsigned.ipa"
            echo "3. Install with AltStore"
          fi
          echo "============================================================"
          echo "SIGNED IPA SIZE:"
          ls -la gymapp2-signed.ipa 2>/dev/null || echo "No signed IPA"
          echo "UNSIGNED IPA SIZE:"
          ls -la gymapp2-unsigned.ipa
          echo "============================================================"

      # ============================================================
      # STEP 23: DEBUG ON FAILURE
      # ============================================================
      - name: Debug on failure
        if: failure()
        run: |
          echo "============================================================"
          echo "❌❌❌ BUILD OR SIGN FAILED ❌❌❌"
          echo "============================================================"
          echo "=== ALL FILES ==="
          ls -la
          echo "=== IOS FOLDER ==="
          ls -la ios/ 2>/dev/null || echo "No ios folder"
          echo "=== APP FILE ==="
          find . -name "*.app" -type d 2>/dev/null
          echo "=== IPA FILES ==="
          find . -name "*.ipa" -type f 2>/dev/null
          echo "=== CERTIFICATE FILES ==="
          ls -la *.p12 2>/dev/null || echo "No .p12 files"
          echo "=== PROFILE FILES ==="
          ls -la *.mobileprovision 2>/dev/null || echo "No .mobileprovision files"
          echo "============================================================"
          echo "📥 Download build-logs to see what went wrong"
          echo "📥 Download build-folder to inspect the build output"
          echo "============================================================"
